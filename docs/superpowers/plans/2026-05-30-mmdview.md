# mmdview Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `.mmd` / `.md` ファイルを監視してリアルタイムで Mermaid 図をプレビューする macOS デスクトップアプリを構築する。

**Architecture:** FastAPI をバックグラウンドスレッドで起動し、pywebview の WKWebView がローカルホストの HTML を表示する。watchdog がファイル変更を検知して SSE 経由でブラウザに通知し、ページをリロードする。mermaid.js（CDN）がクライアントサイドで SVG をレンダリングする。

**Tech Stack:** Python 3.12+, FastAPI, uvicorn, pywebview, watchdog, sse-starlette, Jinja2, htmx, mermaid.js, uv, PyInstaller, Ruff

---

## ファイルマップ

```
mmdview/
├── backend/
│   ├── app.py              # pywebview エントリポイント・ウィンドウ管理・メニュー
│   ├── main.py             # FastAPI アプリ定義・ルーター登録・lifespan
│   ├── paths.py            # アプリデータディレクトリ定数
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── html.py         # GET / (welcome/viewer) + POST /open-file
│   │   └── events.py       # GET /events (SSE ストリーム)
│   ├── services/
│   │   ├── __init__.py
│   │   ├── event_bus.py    # watchdog スレッド → asyncio ブリッジ（pub/sub）
│   │   └── watch_service.py  # watchdog ファイル監視・ファイル内容取得
│   └── templates/
│       ├── base.html       # 共通レイアウト（htmx CDN 含む）
│       ├── welcome.html    # ファイル未選択時の初期画面
│       └── viewer.html     # mermaid.js + SSE クライアント
├── static/
│   └── css/style.css
├── tests/
│   ├── conftest.py         # pytest fixtures（TestClient）
│   ├── unit/
│   │   ├── test_event_bus.py
│   │   └── test_watch_service.py
│   └── integration/
│       ├── test_html_router.py
│       └── test_events_router.py
├── pyproject.toml
└── mmdview.spec            # PyInstaller 設定
```

---

## Task 1: プロジェクトブートストラップ

**Files:**
- Create: `pyproject.toml`
- Create: `backend/__init__.py`
- Create: `backend/routers/__init__.py`
- Create: `backend/services/__init__.py`
- Create: `tests/__init__.py`
- Create: `tests/unit/__init__.py`
- Create: `tests/integration/__init__.py`
- Create: `static/css/style.css` (空)

- [ ] **Step 1: pyproject.toml を作成する**

```toml
[project]
name = "mmdview"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136",
    "uvicorn[standard]>=0.46",
    "pywebview>=6.2",
    "watchdog>=6.0",
    "sse-starlette>=2.1",
    "jinja2>=3.1",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "httpx>=0.28",
    "ruff>=0.9",
    "pyinstaller>=6.20",
]

[tool.taskipy.tasks]
dev = "python -m backend.app"
server = "uvicorn backend.main:app --reload --port 8000"
test = "pytest tests/ -v"
build = "pyinstaller mmdview.spec"
lint = "ruff check . && ruff format --check ."

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "C901"]
mccabe.max-complexity = 10

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

- [ ] **Step 2: ディレクトリ構造とパッケージ init ファイルを作成する**

```bash
mkdir -p backend/routers backend/services backend/templates static/css tests/unit tests/integration
touch backend/__init__.py backend/routers/__init__.py backend/services/__init__.py
touch tests/__init__.py tests/unit/__init__.py tests/integration/__init__.py
touch static/css/style.css
```

- [ ] **Step 3: 依存関係をインストールする**

```bash
uv sync
```

Expected: `Resolved N packages` が表示され `uv.lock` が生成される。

- [ ] **Step 4: コミット**

```bash
git add pyproject.toml uv.lock backend/ static/ tests/
git commit -m "chore: bootstrap mmdview project structure"
```

---

## Task 2: paths.py — アプリデータディレクトリ定義

**Files:**
- Create: `backend/paths.py`
- Test: `tests/unit/test_paths.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/unit/test_paths.py`:

```python
from pathlib import Path


def test_app_data_dir_exists():
    from backend.paths import APP_DATA_DIR
    assert APP_DATA_DIR.exists()
    assert APP_DATA_DIR.is_dir()


def test_window_state_file_path():
    from backend.paths import APP_DATA_DIR, WINDOW_STATE_FILE
    assert WINDOW_STATE_FILE.parent == APP_DATA_DIR
    assert WINDOW_STATE_FILE.name == "window_state.json"
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
uv run pytest tests/unit/test_paths.py -v
```

Expected: `ModuleNotFoundError: No module named 'backend.paths'`

- [ ] **Step 3: 実装する**

`backend/paths.py`:

```python
from pathlib import Path

APP_DATA_DIR = Path.home() / "Library" / "Application Support" / "mmdview"
APP_DATA_DIR.mkdir(parents=True, exist_ok=True)

WINDOW_STATE_FILE = APP_DATA_DIR / "window_state.json"
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
uv run pytest tests/unit/test_paths.py -v
```

Expected: `2 passed`

- [ ] **Step 5: コミット**

```bash
git add backend/paths.py tests/unit/test_paths.py
git commit -m "feat: add app data directory paths"
```

---

## Task 3: EventBus — watchdog スレッド → asyncio ブリッジ

**Files:**
- Create: `backend/services/event_bus.py`
- Test: `tests/unit/test_event_bus.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/unit/test_event_bus.py`:

```python
import asyncio
import pytest
from backend.services.event_bus import EventBus


@pytest.fixture
def bus():
    b = EventBus()
    b.set_loop(asyncio.new_event_loop())
    return b


def test_subscribe_returns_queue(bus):
    q = bus.subscribe()
    assert q is not None


def test_notify_puts_event_in_queue(bus):
    loop = asyncio.new_event_loop()
    bus.set_loop(loop)
    q = bus.subscribe()
    bus.notify("reload")
    event = loop.run_until_complete(asyncio.wait_for(q.get(), timeout=1.0))
    assert event == "reload"
    loop.close()


def test_unsubscribe_removes_queue(bus):
    q = bus.subscribe()
    bus.unsubscribe(q)
    assert q not in bus._listeners
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
uv run pytest tests/unit/test_event_bus.py -v
```

Expected: `ModuleNotFoundError: No module named 'backend.services.event_bus'`

- [ ] **Step 3: 実装する**

`backend/services/event_bus.py`:

```python
import asyncio


class EventBus:
    def __init__(self) -> None:
        self._listeners: list[asyncio.Queue] = []
        self._loop: asyncio.AbstractEventLoop | None = None

    def set_loop(self, loop: asyncio.AbstractEventLoop) -> None:
        self._loop = loop

    def subscribe(self) -> asyncio.Queue:
        q: asyncio.Queue = asyncio.Queue()
        self._listeners.append(q)
        return q

    def unsubscribe(self, q: asyncio.Queue) -> None:
        if q in self._listeners:
            self._listeners.remove(q)

    def notify(self, event: str = "reload") -> None:
        if self._loop is None:
            return
        for q in self._listeners:
            self._loop.call_soon_threadsafe(q.put_nowait, event)


event_bus = EventBus()
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
uv run pytest tests/unit/test_event_bus.py -v
```

Expected: `3 passed`

- [ ] **Step 5: コミット**

```bash
git add backend/services/event_bus.py tests/unit/test_event_bus.py
git commit -m "feat: add EventBus for thread-to-asyncio bridge"
```

---

## Task 4: WatchService — watchdog ファイル監視

**Files:**
- Create: `backend/services/watch_service.py`
- Test: `tests/unit/test_watch_service.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/unit/test_watch_service.py`:

```python
import time
from pathlib import Path
import pytest
from unittest.mock import MagicMock
from backend.services.watch_service import WatchService


@pytest.fixture
def tmp_mmd(tmp_path):
    f = tmp_path / "test.mmd"
    f.write_text("graph TD\n    A --> B", encoding="utf-8")
    return f


def test_get_content_returns_none_when_no_file():
    svc = WatchService()
    assert svc.get_content() is None


def test_get_path_returns_none_when_no_file():
    svc = WatchService()
    assert svc.get_path() is None


def test_set_file_reads_content(tmp_mmd):
    svc = WatchService()
    svc.set_file(str(tmp_mmd))
    assert svc.get_content() == "graph TD\n    A --> B"
    svc.stop()


def test_set_file_sets_path(tmp_mmd):
    svc = WatchService()
    svc.set_file(str(tmp_mmd))
    assert svc.get_path() == tmp_mmd
    svc.stop()


def test_set_file_starts_observer(tmp_mmd):
    svc = WatchService()
    svc.set_file(str(tmp_mmd))
    assert svc._observer is not None
    assert svc._observer.is_alive()
    svc.stop()


def test_stop_kills_observer(tmp_mmd):
    svc = WatchService()
    svc.set_file(str(tmp_mmd))
    svc.stop()
    assert svc._observer is None


def test_notify_called_on_file_change(tmp_mmd):
    from backend.services.event_bus import EventBus
    bus = EventBus()
    notified = []
    bus.notify = lambda event="reload": notified.append(event)

    svc = WatchService(event_bus=bus)
    svc.set_file(str(tmp_mmd))
    tmp_mmd.write_text("graph TD\n    A --> C", encoding="utf-8")
    time.sleep(0.5)
    svc.stop()

    assert "reload" in notified
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
uv run pytest tests/unit/test_watch_service.py -v
```

Expected: `ModuleNotFoundError: No module named 'backend.services.watch_service'`

- [ ] **Step 3: 実装する**

`backend/services/watch_service.py`:

```python
from pathlib import Path
from watchdog.observers import Observer
from watchdog.events import FileSystemEventHandler, FileSystemEvent
from backend.services.event_bus import EventBus, event_bus as _default_bus


class _ChangeHandler(FileSystemEventHandler):
    def __init__(self, target: Path, bus: EventBus) -> None:
        self._target = target
        self._bus = bus

    def on_modified(self, event: FileSystemEvent) -> None:
        if Path(event.src_path) == self._target:
            self._bus.notify()


class WatchService:
    def __init__(self, event_bus: EventBus | None = None) -> None:
        self._bus = event_bus or _default_bus
        self._observer: Observer | None = None
        self._path: Path | None = None

    def set_file(self, path: str) -> None:
        self.stop()
        self._path = Path(path)
        observer = Observer()
        handler = _ChangeHandler(self._path, self._bus)
        observer.schedule(handler, str(self._path.parent), recursive=False)
        observer.start()
        self._observer = observer

    def get_content(self) -> str | None:
        if self._path is None or not self._path.exists():
            return None
        return self._path.read_text(encoding="utf-8")

    def get_path(self) -> Path | None:
        return self._path

    def stop(self) -> None:
        if self._observer is not None:
            self._observer.stop()
            self._observer.join()
            self._observer = None


watch_service = WatchService()
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
uv run pytest tests/unit/test_watch_service.py -v
```

Expected: `7 passed`

- [ ] **Step 5: コミット**

```bash
git add backend/services/watch_service.py tests/unit/test_watch_service.py
git commit -m "feat: add WatchService with watchdog file monitoring"
```

---

## Task 5: FastAPI アプリスケルトン + テスト用 conftest

**Files:**
- Create: `backend/main.py`
- Create: `tests/conftest.py`

- [ ] **Step 1: main.py を作成する**

`backend/main.py`:

```python
import asyncio
from contextlib import asynccontextmanager
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from backend.services.event_bus import event_bus
from backend.routers import html, events


@asynccontextmanager
async def lifespan(app: FastAPI):
    event_bus.set_loop(asyncio.get_event_loop())
    yield


app = FastAPI(lifespan=lifespan)
app.mount("/static", StaticFiles(directory="static"), name="static")
app.include_router(html.router)
app.include_router(events.router)
```

- [ ] **Step 2: conftest.py を作成する**

`tests/conftest.py`:

```python
import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client():
    # routers must exist before importing main
    from backend.main import app
    with TestClient(app) as c:
        yield c
```

- [ ] **Step 3: コミット**

```bash
git add backend/main.py tests/conftest.py
git commit -m "feat: add FastAPI app skeleton and test fixtures"
```

---

## Task 6: HTML ルーター — welcome 画面 + open-file

**Files:**
- Create: `backend/routers/html.py`
- Test: `tests/integration/test_html_router.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/integration/test_html_router.py`:

```python
import pytest
from pathlib import Path
from unittest.mock import patch, MagicMock
from fastapi.testclient import TestClient


@pytest.fixture(autouse=True)
def reset_watch_service():
    from backend.services.watch_service import watch_service
    watch_service.stop()
    watch_service._path = None
    yield
    watch_service.stop()
    watch_service._path = None


def test_index_shows_welcome_when_no_file(client):
    response = client.get("/")
    assert response.status_code == 200
    assert "ファイルを開く" in response.text


def test_index_shows_viewer_when_file_set(client, tmp_path):
    f = tmp_path / "test.mmd"
    f.write_text("graph TD\n    A --> B", encoding="utf-8")
    from backend.services.watch_service import watch_service
    watch_service.set_file(str(f))

    response = client.get("/")
    assert response.status_code == 200
    assert "graph TD" in response.text
    assert "test.mmd" in response.text


def test_open_file_sets_file_and_redirects(client, tmp_path):
    f = tmp_path / "diagram.mmd"
    f.write_text("graph LR\n    X --> Y", encoding="utf-8")

    with patch("backend.routers.html._pick_file", return_value=str(f)):
        response = client.post("/open-file")

    assert response.status_code == 200
    assert response.headers.get("HX-Redirect") == "/"
    from backend.services.watch_service import watch_service
    assert watch_service.get_path() == f


def test_open_file_does_nothing_when_cancelled(client):
    with patch("backend.routers.html._pick_file", return_value=None):
        response = client.post("/open-file")
    assert response.status_code == 200
    assert response.headers.get("HX-Redirect") == "/"
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
uv run pytest tests/integration/test_html_router.py -v
```

Expected: `ModuleNotFoundError: No module named 'backend.routers.html'`

- [ ] **Step 3: 実装する**

`backend/routers/html.py`:

```python
from fastapi import APIRouter, Request
from fastapi.responses import HTMLResponse, Response
from fastapi.templating import Jinja2Templates
from backend.services.watch_service import watch_service

router = APIRouter()
templates = Jinja2Templates(directory="backend/templates")


def _pick_file() -> str | None:
    import webview
    result = webview.windows[0].create_file_dialog(
        webview.OPEN_DIALOG,
        allow_multiple=False,
        file_types=("Mermaid files (*.mmd;*.md)", "All files (*.*)"),
    )
    return result[0] if result else None


@router.get("/", response_class=HTMLResponse)
async def index(request: Request) -> HTMLResponse:
    content = watch_service.get_content()
    path = watch_service.get_path()
    if content is None:
        return templates.TemplateResponse("welcome.html", {"request": request})
    return templates.TemplateResponse("viewer.html", {
        "request": request,
        "content": content,
        "filename": path.name,
        "filepath": str(path),
    })


@router.post("/open-file")
async def open_file() -> Response:
    path = _pick_file()
    if path:
        watch_service.set_file(path)
    return Response(headers={"HX-Redirect": "/"})
```

- [ ] **Step 4: templates ディレクトリに空のスタブテンプレートを置く（Task 8 で本実装）**

```bash
cat > backend/templates/base.html << 'EOF'
<!DOCTYPE html><html><body>{% block content %}{% endblock %}</body></html>
EOF
cat > backend/templates/welcome.html << 'EOF'
{% extends "base.html" %}{% block content %}<button>ファイルを開く</button>{% endblock %}
EOF
cat > backend/templates/viewer.html << 'EOF'
{% extends "base.html" %}{% block content %}<span>{{ filename }}</span><pre class="mermaid">{{ content }}</pre>{% endblock %}
EOF
```

- [ ] **Step 5: テストが通ることを確認する**

```bash
uv run pytest tests/integration/test_html_router.py -v
```

Expected: `4 passed`

- [ ] **Step 6: コミット**

```bash
git add backend/routers/html.py backend/templates/ tests/integration/test_html_router.py
git commit -m "feat: add HTML router with welcome page and file open dialog"
```

---

## Task 7: SSE イベントルーター

**Files:**
- Create: `backend/routers/events.py`
- Test: `tests/integration/test_events_router.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/integration/test_events_router.py`:

```python
import pytest
from fastapi.testclient import TestClient


def test_events_endpoint_exists(client):
    # SSE エンドポイントが存在し、接続できることを確認
    # TestClient は SSE をストリーミングしないため、接続確立のみ検証
    with client.stream("GET", "/events") as response:
        assert response.status_code == 200
        assert "text/event-stream" in response.headers["content-type"]
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
uv run pytest tests/integration/test_events_router.py -v
```

Expected: `FAILED` または `ModuleNotFoundError`

- [ ] **Step 3: 実装する**

`backend/routers/events.py`:

```python
from fastapi import APIRouter, Request
from sse_starlette.sse import EventSourceResponse
from backend.services.event_bus import event_bus

router = APIRouter()


@router.get("/events")
async def sse_endpoint(request: Request) -> EventSourceResponse:
    async def generator():
        q = event_bus.subscribe()
        try:
            while True:
                if await request.is_disconnected():
                    break
                event = await q.get()
                yield {"data": event}
        finally:
            event_bus.unsubscribe(q)

    return EventSourceResponse(generator())
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
uv run pytest tests/integration/test_events_router.py -v
```

Expected: `1 passed`

- [ ] **Step 5: コミット**

```bash
git add backend/routers/events.py tests/integration/test_events_router.py
git commit -m "feat: add SSE events router for file change notifications"
```

---

## Task 8: テンプレートと CSS

**Files:**
- Modify: `backend/templates/base.html`
- Modify: `backend/templates/welcome.html`
- Modify: `backend/templates/viewer.html`
- Modify: `static/css/style.css`

- [ ] **Step 1: base.html を実装する**

`backend/templates/base.html`:

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>mmdview</title>
  <script src="https://unpkg.com/htmx.org@2.0.4" integrity="sha384-HGfztofotfshcF7+8n44JQL2oJmowVChPTg48S+jvZoztPfvwD79OC/LTtG6dMp+" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="/static/css/style.css">
</head>
<body>
  {% block content %}{% endblock %}
</body>
</html>
```

- [ ] **Step 2: welcome.html を実装する**

`backend/templates/welcome.html`:

```html
{% extends "base.html" %}
{% block content %}
<div class="welcome">
  <p class="welcome-message">Mermaid ファイル (.mmd / .md) を開いてください</p>
  <button hx-post="/open-file" hx-swap="none">
    ファイルを開く
  </button>
</div>
{% endblock %}
```

- [ ] **Step 3: viewer.html を実装する**

`backend/templates/viewer.html`:

```html
{% extends "base.html" %}
{% block content %}
<div class="toolbar">
  <span class="filepath" title="{{ filepath }}">{{ filename }}</span>
  <button hx-post="/open-file" hx-swap="none">
    別のファイルを開く
  </button>
</div>
<div class="viewer">
  <pre class="mermaid">{{ content }}</pre>
</div>
<script type="module">
  import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs';
  mermaid.initialize({ startOnLoad: true, theme: 'default' });

  const evtSource = new EventSource('/events');
  evtSource.onmessage = (e) => {
    if (e.data === 'reload') window.location.reload();
  };
</script>
{% endblock %}
```

- [ ] **Step 4: style.css を実装する**

`static/css/style.css`:

```css
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #fff;
}

.welcome {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  gap: 16px;
}

.welcome-message {
  font-size: 14px;
  color: #666;
}

.toolbar {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 16px;
  background: #f5f5f5;
  border-bottom: 1px solid #e0e0e0;
  flex-shrink: 0;
}

.filepath {
  font-size: 13px;
  color: #444;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
  min-width: 0;
}

.viewer {
  flex: 1;
  overflow: auto;
  padding: 32px;
  display: flex;
  align-items: flex-start;
  justify-content: center;
}

button {
  padding: 6px 14px;
  border: 1px solid #ccc;
  border-radius: 6px;
  background: white;
  cursor: pointer;
  font-size: 13px;
  white-space: nowrap;
}

button:hover {
  background: #f0f0f0;
}
```

- [ ] **Step 5: すべてのテストが通ることを確認する**

```bash
uv run pytest tests/ -v
```

Expected: すべてのテストが `passed`

- [ ] **Step 6: コミット**

```bash
git add backend/templates/ static/css/style.css
git commit -m "feat: add templates and CSS for welcome and viewer pages"
```

---

## Task 9: pywebview エントリポイント — app.py

**Files:**
- Create: `backend/app.py`

*注: pywebview は実際の macOS 環境が必要なため、このファイルは自動テスト対象外。手動で動作確認する。*

- [ ] **Step 1: app.py を実装する**

`backend/app.py`:

```python
import json
import socket
import threading
import time
from pathlib import Path

import uvicorn
import webview

from backend.main import app
from backend.paths import WINDOW_STATE_FILE
from backend.services.watch_service import watch_service

_DEFAULT_STATE: dict = {"x": 100, "y": 100, "width": 1024, "height": 768, "last_file": None}


def _load_window_state() -> dict:
    if WINDOW_STATE_FILE.exists():
        try:
            return json.loads(WINDOW_STATE_FILE.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, KeyError):
            pass
    return _DEFAULT_STATE.copy()


def _save_window_state(window: webview.Window) -> None:
    state = {
        "x": window.x,
        "y": window.y,
        "width": window.width,
        "height": window.height,
        "last_file": str(watch_service.get_path()) if watch_service.get_path() else None,
    }
    WINDOW_STATE_FILE.write_text(json.dumps(state), encoding="utf-8")


def _find_free_port() -> int:
    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
        s.bind(("127.0.0.1", 0))
        return s.getsockname()[1]


def _start_server(port: int) -> None:
    uvicorn.run(app, host="127.0.0.1", port=port, log_level="error")


def _open_file_from_menu(window: webview.Window) -> None:
    result = window.create_file_dialog(
        webview.OPEN_DIALOG,
        allow_multiple=False,
        file_types=("Mermaid files (*.mmd;*.md)", "All files (*.*)"),
    )
    if result:
        watch_service.set_file(result[0])
        window.evaluate_js("window.location.reload()")


def main() -> None:
    port = _find_free_port()
    state = _load_window_state()

    if state.get("last_file"):
        watch_service.set_file(state["last_file"])

    server_thread = threading.Thread(target=_start_server, args=(port,), daemon=True)
    server_thread.start()
    time.sleep(0.5)  # サーバー起動待ち

    window = webview.create_window(
        "mmdview",
        f"http://127.0.0.1:{port}/",
        x=state.get("x", 100),
        y=state.get("y", 100),
        width=state.get("width", 1024),
        height=state.get("height", 768),
    )

    _save_timer: threading.Timer | None = None

    def _schedule_save() -> None:
        nonlocal _save_timer
        if _save_timer:
            _save_timer.cancel()
        _save_timer = threading.Timer(0.5, _save_window_state, args=(window,))
        _save_timer.start()

    window.events.moved += lambda x, y: _schedule_save()
    window.events.resized += lambda width, height: _schedule_save()

    menu = [
        webview.Menu("File", [
            webview.MenuAction("Open...", lambda: _open_file_from_menu(window)),
            webview.MenuSeparator(),
            webview.MenuAction("Close", window.destroy),
        ])
    ]

    webview.start(menu=menu)
    watch_service.stop()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 手動でアプリを起動して動作確認する**

```bash
uv run task dev
```

確認事項:
- ウィンドウが開き welcome 画面が表示される
- 「ファイルを開く」ボタンをクリックするとファイルダイアログが開く
- `.mmd` ファイルを選択すると Mermaid 図がレンダリングされる
- ファイルを編集して保存すると自動でリロードされる
- ウィンドウを閉じて再起動すると前回のファイルが表示される
- `⌘O` でファイルダイアログが開く

- [ ] **Step 3: コミット**

```bash
git add backend/app.py
git commit -m "feat: add pywebview entry point with window state and macOS menu"
```

---

## Task 10: PyInstaller スペックと配布ビルド

**Files:**
- Create: `mmdview.spec`

- [ ] **Step 1: mmdview.spec を作成する**

`mmdview.spec`:

```python
# -*- mode: python ; coding: utf-8 -*-
from pathlib import Path

block_cipher = None

a = Analysis(
    ['backend/app.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('backend/templates', 'backend/templates'),
        ('static', 'static'),
    ],
    hiddenimports=[
        'backend.routers.html',
        'backend.routers.events',
        'backend.services.event_bus',
        'backend.services.watch_service',
        'backend.paths',
        'uvicorn.logging',
        'uvicorn.loops',
        'uvicorn.loops.auto',
        'uvicorn.protocols',
        'uvicorn.protocols.http',
        'uvicorn.protocols.http.auto',
        'uvicorn.protocols.websockets',
        'uvicorn.protocols.websockets.auto',
        'uvicorn.lifespan',
        'uvicorn.lifespan.on',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='mmdview',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
)

coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='mmdview',
)

app = BUNDLE(
    coll,
    name='mmdview.app',
    icon=None,
    bundle_identifier='com.degino.mmdview',
    info_plist={
        'NSHighResolutionCapable': True,
        'CFBundleShortVersionString': '0.1.0',
    },
)
```

- [ ] **Step 2: ビルドを実行する**

```bash
uv run task build
```

Expected: `dist/mmdview.app` が生成される

- [ ] **Step 3: ビルドされたアプリを起動して動作確認する**

```bash
open dist/mmdview.app
```

確認事項: Task 9 Step 2 と同じチェックリストをビルド版で確認する。

- [ ] **Step 4: コミット**

```bash
git add mmdview.spec
git commit -m "chore: add PyInstaller spec for macOS app bundle"
```

---

## 完了チェックリスト

- [ ] `uv run pytest tests/ -v` で全テスト通過
- [ ] `uv run task dev` でアプリが起動し Mermaid 図がレンダリングされる
- [ ] ファイル保存で自動リロードされる
- [ ] ウィンドウ状態が再起動後に復元される
- [ ] `uv run task build` で `.app` バンドルが生成される
